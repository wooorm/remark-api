# Smol package

## API

### `sum(a, b) (default)`

Add two numbers together.

###### Parameters

* `a` (`number`)
  — the first number
* `b` (`number`)
  — the second number

###### Returns

The sum of `a` and `b` (`number`).
