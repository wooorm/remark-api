/**
 * Add two numbers together.
 *
 * @param {number} a
 *   The first number.
 * @param {number} b
 *   The second number.
 * @returns {number}
 *   The sum of `a` and `b`.
 */
export default function sum(a, b) {
  return a + b
}
